"""
frame_renderer.py

Placeholder module. Frame-triad rendering currently lives inline in
RobotRenderer for Phase 1; it will be factored out here once the
frame-display feature set grows (custom scaling, labels, toggling per
frame) in a later phase.
"""
