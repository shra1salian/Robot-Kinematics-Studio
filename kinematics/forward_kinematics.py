"""
forward_kinematics.py

Placeholder module. The generic, chain-agnostic forward-kinematics
engine (Section 10 of the design spec) is implemented in Phase 3.
Phase 1's demo robot computes its own FK inline via
RobotModel.compute_frames(), built on the same transformation
primitives this module will use, so migrating later will not require
touching the robot model's public interface.
"""
